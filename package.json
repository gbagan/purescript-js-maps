{
  "scripts": {
    "test": "spago -x test.dhall test",
    "benchmark": "spago -x benchmark.dhall build && purs-backend-es bundle-app --no-build -m Benchmark.Main && node index.js"
  },
  "dependencies": {
    "purs-backend-es": "^1.3.2",
    "spago": "^0.20.9"
  }
}
